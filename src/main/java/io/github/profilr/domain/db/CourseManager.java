package io.github.profilr.domain.db;

public class CourseManager {

	/* TODO Need some methods to add courses to the db and to find courses by their primary keys.
	 * I think the only time we'll need to 'find a course' is when someone is enrolling in a course and their user object
	 * does not already have a pointer to the course we need to find.
	 */
	
}
